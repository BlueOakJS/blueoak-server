exports.metadata = {
    id: "express",
    description: "Logging service",
    dependencies: ['config']
};

var _ = require('lodash');

exports.init = function(server, cfg, callback) {

    callback();
};