get:
  summary: Get Policy Details
  tags:
    - Policies
  parameters:
    - $ref: '../parameters/PathId.yaml'
  responses:
    200:
      schema:
        $ref: '../definitions/Policy.yaml'
      description: give me a friggen policy
