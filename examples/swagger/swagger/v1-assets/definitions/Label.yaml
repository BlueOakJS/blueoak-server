discriminator: label_type
required:
      - label_type
      - name

properties:
  name:
    type: string
    description: |
      Unique identifier representing a specific product for a given latitude & longitude.
      For example, uberX in San Francisco will have a different product_id than uberX in Los Angeles.