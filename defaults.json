{
  "logger": {
    "default_levels": ["DEBUG", "INFO", "WARN", "ERROR"],
    "color": true,
    "showPID": "auto", //auto will check for the worker count
    "timestamp": "iso" //ether iso, none/false, ms
  },

  "cluster": {
    "maxWorkers": -1 //-1 means use full cpu count
  },

  "session": {
    "type": "cookie"
  },

  "express": {
    "port": 3000
  },

  "bodyParser": {
  }
}